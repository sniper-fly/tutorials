{
  "name": "lambda",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "rm -rf dist && esbuild ./src/* --entry-names=[dir]/index --bundle --minify --sourcemap --platform=node --target=node16.14 --outdir=dist"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "aws-lambda": "^1.0.7",
    "axios": "^1.6.8"
  },
  "devDependencies": {
    "esbuild": "^0.20.2"
  }
}
